import Link from "next/link";

export const metadata = { title: "教師帳號設定｜創客學院" };

export default function TeacherGuidePage() {
  return <main className="min-h-screen bg-[#f5f7f4] text-slate-900"><header className="border-b border-slate-200 bg-white"><div className="mx-auto flex max-w-4xl items-center justify-between px-5 py-4 lg:px-8"><Link href="/teacher" className="font-semibold text-teal-700">← 返回教師後台</Link><Link href="/app" className="text-sm font-semibold text-teal-700">學員端</Link></div></header><div className="mx-auto max-w-4xl px-5 py-12 lg:px-8"><p className="text-sm font-bold uppercase tracking-[0.2em] text-teal-700">Teacher access setup</p><h1 className="mt-4 text-4xl font-bold tracking-tight">如何安全新增教師權限帳號</h1><p className="mt-5 text-lg leading-8 text-slate-600">教師帳號要先建立一般登入帳號，再由 Supabase 管理者把該帳號的 profile role 設為 teacher。系統不允許學員自行把自己升級成教師。</p><div className="mt-10 space-y-5"><article className="rounded-3xl bg-white p-6 shadow-sm ring-1 ring-slate-200"><span className="text-sm font-bold text-teal-700">01</span><h2 className="mt-2 text-xl font-bold">先建立登入帳號</h2><p className="mt-3 leading-7 text-slate-600">在 Supabase Dashboard 的 Authentication → Users → Add user 建立教師 Email 帳號。建議使用公司或學院專用信箱，不要共用私人帳號。</p></article><article className="rounded-3xl bg-white p-6 shadow-sm ring-1 ring-slate-200"><span className="text-sm font-bold text-teal-700">02</span><h2 className="mt-2 text-xl font-bold">確認 profile 已建立</h2><p className="mt-3 leading-7 text-slate-600">第一次登入後，系統通常會依現有 auth callback 建立 profiles 記錄。若沒有建立，請先讓該帳號登入一次，再執行下一步。</p></article><article className="rounded-3xl bg-white p-6 shadow-sm ring-1 ring-slate-200"><span className="text-sm font-bold text-teal-700">03</span><h2 className="mt-2 text-xl font-bold">由管理者執行角色更新</h2><p className="mt-3 leading-7 text-slate-600">在 Supabase SQL Editor 執行以下指令，將 UUID 換成 Authentication → Users 中該教師帳號的 User UID：</p><pre className="mt-4 overflow-x-auto rounded-2xl bg-slate-900 p-4 text-sm leading-7 text-slate-100"><code>{"update public.profiles\nset role = 'teacher', updated_at = now()\nwhere id = 'TEACHER_USER_UUID';"}</code></pre><p className="mt-3 text-sm leading-6 text-amber-800">請不要把 service_role key 放進前端、GitHub 或 Vercel 公開環境變數。角色更新只能由受信任的 Supabase 管理者執行。</p></article><article className="rounded-3xl bg-white p-6 shadow-sm ring-1 ring-slate-200"><span className="text-sm font-bold text-teal-700">04</span><h2 className="mt-2 text-xl font-bold">重新登入並驗證權限</h2><p className="mt-3 leading-7 text-slate-600">教師登出後重新登入，開啟 <code>/teacher</code>。如果看到「此頁面僅限教師使用」，請確認 UUID、profiles.role、Supabase migration 是否已完成。</p></article></div><div className="mt-8 rounded-2xl bg-teal-50 p-5 text-sm leading-7 text-teal-950"><strong>權限原則：</strong>教師可以查看學員 profile、讀取提交、批改、匯入作業並代學員建立 submission；學員只能管理自己的答案與提交。</div></div></main>;
}
